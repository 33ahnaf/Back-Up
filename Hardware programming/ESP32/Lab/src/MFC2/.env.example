# Copy this to .env for local testing (Render/Railway: set these as dashboard env vars instead)
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx.neon.tech/neondb?sslmode=require
DEVICE_API_KEY=pick-a-long-random-secret
PORT=3000
