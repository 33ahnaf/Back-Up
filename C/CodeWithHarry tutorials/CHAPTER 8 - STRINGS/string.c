#include <stdio.h>

int main(){
    // char name[] = {'A', 'h', 'n', 'a', 'f', '\0'};
    char name[] = "Ahnaf"; // Same as the upper one.

    // for (int i = 0; i < 5; i++)
    // {
    //     printf("%c", name[i]);
    // }

    printf("%s", name); // You don't have to run a for loop, just printf it like this!
    
    return 0;
}