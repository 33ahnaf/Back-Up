plugins {
    id 'com.android.application'
}

android {
    namespace 'com.example.volumebooster'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.volumebooster"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildFeatures {
        buildConfig true
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}