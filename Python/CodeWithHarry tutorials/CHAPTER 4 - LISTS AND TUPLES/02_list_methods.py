friends = ["Ahnaf", "Afrina", "Saad"]

print(friends)

friends.append("Muyaz")

print(friends)

# • l1.sort(): updates the list to [1,2,7,8,15,21]
# • l1.reverse(): updates the list to [15,21,2,7,8,1]
# Note: sort() only sorts elements by increasing order and reverse() reverses the list (like this --> [7, 9, 5] to [5, 9, 7] ), however you can sort() and then reverse() for sorting elements by drecesing order.
# • l1.append(8): adds 8 at the end of the list
# • l1.insert(3,8): This will add 8 at 3 index
# • l1.pop(2): Will delete element at index 2 and return its value.
# Note: pop() function delete element at index X and returns it, means we can also see what we have deleted.
# • l1.remove(21): Will remove 21 from the list.